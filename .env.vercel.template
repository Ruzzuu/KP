# ============================================
# 🔐 VERCEL ENVIRONMENT VARIABLES
# ============================================
# PENTING: Set semua variables ini di Vercel Dashboard
# Location: Settings → Environment Variables
#
# CARA SETUP:
# 1. Login ke https://vercel.com
# 2. Pilih project Anda
# 3. Go to Settings → Environment Variables
# 4. Copy-paste setiap variable di bawah ini
# 5. Set untuk: Production, Preview, Development
# ============================================

# ===== FRONTEND ENVIRONMENT VARIABLES =====
# Variables dengan prefix VITE_ akan diexpose ke browser
# Pastikan TIDAK ada data sensitif di sini!

# API Configuration
VITE_API_BASE_URL=https://YOUR-PROJECT-NAME.vercel.app/api

# EmailJS Configuration
# ⚠️ GANTI dengan credentials EmailJS Anda yang sebenarnya
# Dapatkan dari: https://dashboard.emailjs.com/
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxxxxxxxxx

# EmailJS Additional Templates
VITE_EMAILJS_ADMIN_TEMPLATE_ID=template_admin_xxx
VITE_EMAILJS_APPROVAL_TEMPLATE_ID=template_approval_xxx
VITE_EMAILJS_REJECTION_TEMPLATE_ID=template_rejection_xxx

# Admin Configuration
VITE_ADMIN_EMAIL=admin@pergunu.com
VITE_ADMIN_USERNAME=admin

# Security Configuration
VITE_BCRYPT_SALT_ROUNDS=12
VITE_MAX_LOGIN_ATTEMPTS=5
VITE_SESSION_TIMEOUT=3600000

# Demo Mode (set false untuk production)
VITE_DEMO_MODE=false

# ===== BACKEND ENVIRONMENT VARIABLES =====
# Variables tanpa prefix VITE_ hanya tersedia di server-side

# Node Environment
NODE_ENV=production

# CORS Configuration
# ⚠️ GANTI dengan URL Vercel Anda yang sebenarnya setelah deploy
ALLOWED_ORIGINS=https://YOUR-PROJECT-NAME.vercel.app
FRONTEND_URL=https://YOUR-PROJECT-NAME.vercel.app

# Server Configuration
PORT=3001

# ===== OPTIONAL SERVICES =====
# Uncomment jika Anda menggunakan services ini

# Database (jika migrasi dari db.json ke database cloud)
# DATABASE_URL=your_mongodb_or_postgres_url

# File Storage (jika menggunakan cloud storage)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# Or AWS S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_S3_BUCKET=your_bucket_name
# AWS_REGION=your_region

# Or Vercel Blob
# BLOB_READ_WRITE_TOKEN=your_vercel_blob_token

# Rate Limiting
RATE_LIMIT_RPM=100
RATE_LIMIT_WINDOW=15

# Logging
LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=true

# Security Headers
CSP_ENABLED=true
HSTS_ENABLED=true
X_FRAME_OPTIONS=DENY

# ============================================
# 📝 CATATAN PENTING:
# ============================================
# 1. JANGAN commit file ini ke Git jika sudah diisi dengan nilai sebenarnya
# 2. File ini hanya sebagai TEMPLATE/REFERENSI
# 3. Set semua variables di Vercel Dashboard, bukan di file
# 4. Setelah deploy pertama, update VITE_API_BASE_URL dengan URL Vercel Anda
# 5. Update ALLOWED_ORIGINS dan FRONTEND_URL dengan URL Vercel Anda
# ============================================
